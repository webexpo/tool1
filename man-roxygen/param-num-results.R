#' @param num_results A [shiny::reactive()] object returning many outputs
#'   derived from simulated values (`bayesian_analysis`) and parameters
#'   (`parameters`), i.e. the output of external function [all.numeric()].
